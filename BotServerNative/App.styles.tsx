import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  header: {
    margin: 10,
    color: 'blue',
    fontWeight: 'bold',
    fontSize: 30,
  },
  errorText: { // Add this style
    color: 'red',
    fontSize: 16,
    marginTop: 10,
  },
});
